/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["encrypted-tbn0.gstatic.com", "example.com"], // Add any other domains you want to allow
  },
};

export default nextConfig;
